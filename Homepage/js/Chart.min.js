//Current Value----------------------------------------
// Lấy thẻ canvas
var ctx1 = document.getElementById("barChart").getContext("2d");

// Dữ liệu biểu đồ
var data = {
  labels: ["Temperature (°C)", "Infrared (m)", "Light (lm)"],
  datasets: [
    {
      data: [40, 90, 20], // Giá trị của từng màu
      backgroundColor: ["#FF6347", "#00FFFF", "#FFFF66"],
    },
  ],
};

// Cấu hình biểu đồ
var options = {
  indexAxis: "y",
  responsive: true,
  scales: {
    x: {
      grid: {
        color: "rgb(255, 255, 255)",
      },
      ticks: {
        color: "#FFFFFF", // Màu chữ trục X
        font: {
          size: 25, // 🔹 Tăng kích thước chữ trục X
        },
      },
    },
    y: {
      grid: {
        color: "rgb(255, 255, 255)",
      },
      ticks: {
        color: "#FFFFFF", // Màu chữ trục Y
        font: {
          size: 13, // 🔹 Tăng kích thước chữ trục Y
        },
      },
    },
  },
  plugins: {
    legend: {
      display: true,
      labels: {
        color: "#FFFFFF",
        font: {
          size: 16,
        }, // 🔹 Tăng kích thước chữ legend // 🟢 Thêm dòng này để đổi màu chữ label thành trắng
        generateLabels: function (chart) {
          let labels = chart.data.datasets[0].backgroundColor.map(
            (color, index) => ({
              text: chart.data.labels[index],
              fillStyle: color,
              hidden: false,
              fontColor: "#FFFFFF", // 🟢 Đổi màu chữ của label thành trắng
            })
          );
          return labels;
        },
      },
    },
  },
};

// Vẽ biểu đồ
new Chart(ctx1, {
  type: "bar",
  data: data,
  options: options,
});

//History value----------------------------------------
const ctx2 = document.getElementById("lineChart").getContext("2d");

const historyChart = new Chart(ctx2, {
  type: "line",
  data: {
    labels: ["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"], // 7 ngày trong tuần
    datasets: [
      {
        label: "Temperature (°C)",
        data: [10, 30, 25, 40, 35, 50, 45], // Dữ liệu 1
        borderColor: "red",
        backgroundColor: "rgba(255, 0, 0, 0.2)",
        borderWidth: 2,
        tension: 0.3,
      },
      {
        label: "Infrared (m)",
        data: [20, 25, 30, 45, 40, 55, 60], // Dữ liệu 2
        borderColor: "aqua",
        backgroundColor: "rgba(0, 255, 255, 0.2)",
        borderWidth: 2,
        tension: 0.3,
      },
      {
        label: "Light (lm)",
        data: [15, 35, 20, 50, 30, 45, 55], // Dữ liệu 3
        borderColor: "yellow",
        backgroundColor: "rgba(255, 255, 0, 0.2)",
        borderWidth: 2,
        tension: 0.3,
      },
    ],
  },
  options: {
    responsive: true,
    maintainAspectRatio: false,
    scales: {
      x: {
        ticks: {
          color: "white", // Màu chữ trục X
          font: {
            size: 16, // 🔹 Tăng kích thước chữ trục X
          },
        },
      },
      y: {
        ticks: {
          color: "white", // Màu chữ trục Y
        },
      },
    },
    plugins: {
      legend: {
        labels: {
          color: "white", // Màu chữ label
          font: { size: 14 }, // Cỡ chữ label
        },
      },
    },
  },
});
